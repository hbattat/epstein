version: '3.8'

services:
  videovault:
    build: .
    container_name: videovault
    ports:
      - "3000:3000"
    volumes:
      - ./data:/app/data
    networks:
      - npm
    restart: unless-stopped
    environment:
      - NODE_ENV=production

networks:
  npm:
    external: true
