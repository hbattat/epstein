version: '3.8'

services:
  videovault:
    build: .
    container_name: videovault
    ports:
      - "3000:3000"
    volumes:
      - ./data:/app/data
    restart: unless-stopped
    environment:
      - NODE_ENV=production
